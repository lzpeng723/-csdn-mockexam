version: "3.1"
services:
  # 主容器
  web:
    build: .
    restart: always
    ports:
      - 8080:8080
    depends_on:
      - db
  # JDK8及以上版本
  adminer:
    image: adminer
    restart: always
    ports:
      - 9090:8080
    depends_on:
      - db
  # MySQL5.7及以上版本
  db:
    image: mysql
    command: --default-authentication-plugin=mysql_native_password
    restart: always
    environment:
      MYSQL_DATABASE: csdn-mockexam-c4-javabase
      MYSQL_USER: csdn-mockexam-c4-javabase
      MYSQL_PASSWORD: csdn-mockexam-c4-javabase
      MYSQL_RANDOM_ROOT_PASSWORD: yes
